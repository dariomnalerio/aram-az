/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: "tyvfupwimjvojspogowm.supabase.co",
      },
    ],
  },
};

export default nextConfig;

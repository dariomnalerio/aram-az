export type Option = {
  value: string;
  label: string;
};

export type UserClub = {
  user_id: string;
  club_id: string;
}

export type ChampImg = {
  id: string;
  img_url: string;
  name: string;
}